---
-   hosts: lablon16 
    gather_facts: False
    vars_files:
       -  vars/lab16.yml
    roles:
       - { role: createvms }

# Group postinstall is dynamically generated by createvms role
-   hosts: postinstall
    gather_facts: False
    pre_tasks:
       - setup:
            filter: ansible_*
    vars_files:
       -  vars/lab16.yml
    roles:
       - { role: postinstallvm }
